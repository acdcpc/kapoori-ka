// src/config/featureFlags.ts — single place to switch features on/off.

// हेरचाह टोली (Care team & care log) — hidden at launch, planned for the
// next update. Flip to true to re-enable the dashboard tile, the Settings
// card, and the shared-records flows.
export const FEATURE_CARE_TEAM = false;
