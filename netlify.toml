# Netlify config — Kapoori Ka web pages (payment + admin + guide)
[build]
  # No build step: these are static HTML files served straight from public/
  command = ""
  publish = "public"

# Make the bare root URL land on the public payment page instead of 404ing
[[redirects]]
  from = "/"
  to = "/payment.html"
  status = 200
